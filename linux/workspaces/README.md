# Workspaces

This directory is for local inputs, generated media, and scratch experiments for
Linux utilities.

Git ignores workspace contents by default, except `README.md` files. Keep
reusable code in `../scripts/`, and only promote durable examples by adding
explicit `.gitignore` exceptions.
